# Review Or Generate Experiment Plan

Decide whether an existing plan can be reused or whether a new plan must be generated.

Required actions:

- Read the current experiment plan if it exists.
- Generate a new plan only when needed.
- Record the chosen plan source and path in context.
