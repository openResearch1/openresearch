# Launch Experiment

Start the experiment using the resolved environment, resources, and runtime configuration.

Required actions:

- Launch the experiment with the final arguments.
- Record launch results in context.
- If the run fails and recovery is needed, use `edit` to insert predefined retry or failure-report steps.
