# Confirm Required Configurations

Verify the remote server configuration and W&B configuration before continuing.

Required actions:

- Confirm required runtime credentials and target environment details.
- If user input is missing, use `wait_interaction` and clearly ask for it.
- Update workflow context with resolved configuration values.
