# Prepare Remote Resources

Resolve datasets, models, checkpoints, and any other runtime resources.

Required actions:

- Determine which resources are needed.
- Resolve remote or reusable paths.
- If runtime findings require extra remedial steps, use `edit` to insert predefined kinds only.
