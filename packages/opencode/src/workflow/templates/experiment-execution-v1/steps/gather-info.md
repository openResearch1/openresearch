# Gather Experiment Information

Collect the experiment metadata and any reusable runtime information needed for later steps.

Required actions:

- Resolve experiment identifiers and code location.
- Check whether prior successful runtime configuration can be reused.
- Record all confirmed inputs into workflow context.
