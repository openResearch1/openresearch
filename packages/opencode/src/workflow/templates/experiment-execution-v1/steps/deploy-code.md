# Deploy Code To Remote Server

Synchronize code changes to the target environment when remote deployment is required.

Required actions:

- Decide whether deployment is necessary.
- Invoke the deployment path when code sync is required.
- Record deployment status in context if relevant.
