# Prepare Remote Environment

Prepare or reuse the target environment before running the experiment.

Required actions:

- Check whether the environment can be reused.
- Run environment setup only if reuse is unsafe or impossible.
- Store the effective runtime environment information in context.
