# Commit Code Changes

Create a commit if the workflow changed code and a commit is appropriate.

Required actions:

- Check whether code actually changed.
- Commit only when that is requested or expected by the workflow policy.
- Do not invent a commit if there are no relevant changes.
