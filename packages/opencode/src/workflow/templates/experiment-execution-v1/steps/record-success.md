# Record Successful Runtime Setup

Persist the effective environment and resource information from a successful run.

Required actions:

- Save the actual runtime environment that succeeded.
- Save reusable resource paths.
- Update context so future runs can reuse this information safely.
